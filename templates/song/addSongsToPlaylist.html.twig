// FIXME: not adding songs in db

{{ form_start(form, {'action': path('add_toSongsPlaylist', {'id': song.id})} )}}


    {# {{form_row(formAddPlaylist.playlistName)}}
    {{form_row(formAddPlaylist.image)}} #}
    {# {{form_row(formAddPlaylist.user)}} #}
    {{form_row(form.playlists)}}
    {{form_row(form.submit)}}

    {{ dump(playlists) }}


{{form_end(form)}}
